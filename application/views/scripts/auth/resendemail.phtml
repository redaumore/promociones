<?php
    $messages = $this->form->getMessages();
    $this->form->setAction($this->url());
    $form = $this->form;
?>
<form method="<?php echo $form->getMethod() ?>" action="<?php echo $form->getAction()?>" class="form-horizontal">
    <div class="row-fluid">
        <div class="row pagination-centered">
            <h1>Registrarse</h1>
        </div>
        <hr class="hr-subtitle"/>
        <?php echo $this->errorMessage($this->form, $messages); ?>
        <div class="row pagination-centered">
            <p>Aún no has activado tu cuenta. Si quieres que se te re-envíe el email de activación, pulsa el botón de abajo.</p>
        </div>
        <div class="row pagination-centered">
            <?php echo $form->reenviar->renderViewHelper();?>
        </div>
    </div>
    <?php echo $form->email->renderViewHelper(); ?>
</form>
